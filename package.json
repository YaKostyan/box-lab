{
  "name": "toffipacks-prototype",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@11.16.0",
  "scripts": {
    "dev": "vite",
    "server": "node server/index.mjs",
    "server:test": "node --experimental-strip-types --test server/*.test.mjs",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types --test scripts/*.test.mjs server/*.test.mjs",
    "check": "tsc --noEmit",
    "standalone": "npm run build && node scripts/make-standalone.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.0.5"
  }
}
